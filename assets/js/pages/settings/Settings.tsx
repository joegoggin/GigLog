import MainLayout from "@/layouts/MainLayout";

const SettingsPage: React.FC = () => {
    return (
        <MainLayout className="settings-page" title="Settings">
            <h1>Settings</h1>
        </MainLayout>
    );
};

export default SettingsPage;
